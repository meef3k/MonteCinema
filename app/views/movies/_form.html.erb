<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row my-2">
    <%= form.label :title, style: "display: block", class: "col-sm-2 col-form-label" %> 
    <div class="col-sm-10">
      <%= form.text_field :title, class: "form-control", placeholder: "Enter title" %>
    </div>
  </div>

  <div class="form-group row my-2">
    <%= form.label :description, style: "display: block", class: "col-sm-2 col-form-label" %> 
    <div class="col-sm-10">
      <%= form.text_field :description, class: "form-control", placeholder: "Enter description" %>
    </div>
  </div>

  <div class="form-group row my-2">
    <%= form.label :duration, style: "display: block", class: "col-sm-2 col-form-label" %> 
    <div class="col-sm-10">
      <%= form.text_field :duration, class: "form-control", placeholder: "Enter duration" %>
    </div>
  </div>

  <div class="col-auto my-2">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>

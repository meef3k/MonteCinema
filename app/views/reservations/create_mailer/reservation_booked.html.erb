<h1>Your reservation with ID: #<%= @reservation.id %> was succesffuly booked</h1>
<h2><%= @reservation.seance.movie.title %></h2>
<h3><%= (@reservation.seance.starts_at).strftime('%H:%M - %e %B') %></h3>
<h4><%= @reservation.seance.hall.name %></h4>
<h5>Seats: <%= @reservation.tickets.map(&:seat).join(", ") %></h5>

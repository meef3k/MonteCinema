Your reservation with ID: #<%= @reservation.id %> was cancelled
<%= @reservation.seance.movie.title %>
<%= (@reservation.seance.starts_at).strftime('%H:%M - %e %B') %>
<%= @reservation.seance.hall.name %>
